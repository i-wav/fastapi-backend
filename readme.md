# FastAPI Backend Deployment with GitHub Actions

## Overview

This project implements a **simple backend API** using **Python and FastAPI** and demonstrates an **automated CI/CD deployment pipeline** using **GitHub Actions**.

The API exposes a single endpoint `/sayHello` and is automatically deployed to a virtual machine on every push to the `main` branch.

---
